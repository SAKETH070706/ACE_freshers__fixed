# API base URL for registration backend.
# In local development with Vite dev server proxying /api to http://localhost:5000,
# you can leave this blank or unset.
# In production or custom deployed environments, set to your backend URL:
# VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=http://localhost:5000
